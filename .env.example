# docker-compose.yml が参照する変数。このリポジトリをクローンした
# ホスト側の絶対パスを指定する（$HOME や相対パスでは解決できない箇所があるため必須）。
#
# 使い方:
#   cp .env.example .env
#   # .env を開いて HOST_PROJECT_DIR を実際の絶対パスに書き換える
#   docker compose up -d
HOST_PROJECT_DIR=/absolute/path/to/claude-containers
